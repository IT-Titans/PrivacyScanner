<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    
    <SolidColorBrush x:Key="PrimaryColor" Color="#182334" />
    <SolidColorBrush x:Key="SecondaryColor" Color="#077aba" />
    <SolidColorBrush x:Key="WarningColor" Color="#FF4040" />
    <SolidColorBrush x:Key="FloatingColor" Color="#1d2b40" />
    
    <SolidColorBrush x:Key="TextColor" Color="#FFFFFF" />
    <SolidColorBrush x:Key="ProgressValueColor" Color="#38BDF8" />
    <SolidColorBrush x:Key="ProgressTrackColor" Color="#0F172A" /> 

    <Style x:Key="AppPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
        <Setter Property="Background" Value="{StaticResource SecondaryColor}" />
        <Setter Property="BorderBrush" Value="{StaticResource SecondaryColor}" />
        <Setter Property="Margin" Value="4" />
        <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
    </Style>
    
    <Style x:Key="AppStopButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
        <Setter Property="BorderBrush" Value="#FFFFFF" />
        <Setter Property="Margin" Value="16,0,0,0" />
        <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
    </Style>
    
    <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
        <Setter Property="Foreground" Value="{StaticResource SecondaryColor}"/>
    </Style>
    
    <Style x:Key="AppFloatingTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}">
        <Setter Property="Margin" Value="0,8" />
    </Style>

    <Style x:Key="AppGroupBoxStyle" TargetType="GroupBox" BasedOn="{StaticResource MaterialDesignCardGroupBox}">
        <Setter Property="Margin" Value="4" />
        <md:ColorZoneAssist.Mode>PrimaryMid</md:ColorZoneAssist.Mode>
    </Style>
    
    <Style x:Key="RoundedActionButton"
           TargetType="Button"
           BasedOn="{StaticResource MaterialDesignRaisedButton}">

        <!-- Grundfarbe & Text -->
        <Setter Property="Background" Value="{StaticResource FloatingColor}" />
        <Setter Property="Foreground" Value="{StaticResource SecondaryColor}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="16,6" />
        <Setter Property="Height" Value="36" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="md:ButtonAssist.CornerRadius" Value="18" />

        <!-- Hover / Pressed über Trigger -->
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource SecondaryColor}" />
                <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource SecondaryColor}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.6" />
            </Trigger>
        </Style.Triggers>

    </Style>
    
    <Style x:Key="AppProgressBarStyle"
           TargetType="ProgressBar"
           BasedOn="{StaticResource MaterialDesignLinearProgressBar}">
        <Setter Property="Height" Value="30"/>
        <Setter Property="Background" Value="{StaticResource ProgressTrackColor}"/>
        <Setter Property="Foreground" Value="{StaticResource ProgressValueColor}"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>


</ResourceDictionary>
