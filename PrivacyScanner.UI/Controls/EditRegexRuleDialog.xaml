<UserControl x:Class="ITTitans.PrivacyScanner.UI.Controls.EditRegexRuleDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewModels="clr-namespace:ITTitans.PrivacyScanner.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400"
             d:DataContext="{d:DesignInstance Type=viewModels:EditRegexRuleDialogViewModel}">
    <UserControl.Resources>
        <md:NullableToVisibilityConverter x:Key="NullableToVisibilityConverter" />
    </UserControl.Resources>
    <Grid Margin="24" Width="350">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Regex Regel editieren" 
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}" 
                   Margin="0,0,0,16"/>

        <TextBox Grid.Row="1" 
                 Text="{Binding RuleName, UpdateSourceTrigger=PropertyChanged}"
                 md:HintAssist.Hint="Regelname"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                 Margin="0,0,0,8"/>

        <TextBox Grid.Row="2" 
                 Text="{Binding Regex, UpdateSourceTrigger=PropertyChanged}"
                 md:HintAssist.Hint="Regex"
                 Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                 Margin="0,0,0,16"/>

        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" 
                   Foreground="Red" 
                   TextWrapping="Wrap"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullableToVisibilityConverter}}"
                   Margin="0,0,0,16"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Command="{Binding CancelCommand}" 
                    Content="Abbrechen" 
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Margin="0,0,8,0"/>
            <Button Command="{Binding UpdateCommand}" 
                    Content="Ändern" 
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
